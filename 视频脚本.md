# 2 分钟演示视频脚本（≤120 秒，mp4 ≤200MB）

> 目标：展示 agent 完成一个**真实多文件任务**（含调试+开发），并讲清"它为什么这样运转"。
> 录制环境建议：Windows Terminal + 等宽字体 + 深色主题；`coding-agent chat` 交互式演示；可 1.5-2 倍速播放关键环节（题目允许剪辑与加速）；字幕标注工具调用；全程不出现 API key。

## 时间轴

| 时间 | 画面/操作 | 旁白（中文） |
|---|---|---|
| 0:00-0:08 | 开场：终端 `coding-agent chat`，气泡输入 | 这是我完全自研的编程智能体：自己读文件、改代码、跑命令，直到完成任务。全流程无任何 agent 框架。 |
| 0:08-0:20 | 下层多文件 bug 修复任务（加速）：read→edit→bash pytest→最终回答 | 看它自主闭环；`/cost` 显示用了多少 token/步；`/undo` 能一键撤销它的改动。 |
| 0:20-0:30 | `/codemap` 展示 Aider 式 repo-map（符号地图）+ 追问"改了哪些文件" | 它用紧凑代码地图定位，不用重读全库；`/task` 维护任务清单、`/compact` 压缩长会话。 |
| 0:30-0:50 | 架构图：system prompt+history→LLM→解析→工具执行→回填→终止→循环 | 核心循环自研：上下文管理、工具定义与本地执行、模型输出解析、循环终止、错误处理——都是题目要求、全部自己实现。 |
| 0:50-1:15 | 同一会话输入 `/pm` 进入 PM 演示模式：说想法→agent 澄清一次一问→写 spec→确认→MVP→story/pitch | 最有我的创意：把 PM 流程作为 agent 的一个 **`/pm` 模式**（不是新 agent）——先澄清、再确认、做最小可运行、再产出 spec/story/pitch 四件交付物。这是别的 agent 没有的。 |
| 1:15-1:45 | 切到评测：`python -m eval tasks`（6 梯度 PASS）+ `run_bigcodebench --limit 30`（40% 报告）+ LLM judge | 最特别的是自带的 AI 评测：隐藏测试判 PASS/FAIL、LLM 打质量分、记录成本/耗时/token——我用 BigCodeBench（30 题 40%）和 6 级梯度任务，用数据证明它行不行。 |
| 1:45-2:00 | 收尾：一句总结 + 演进 | 自研闭环、可评测、可辩护；还有 PM 演示模式这类因人而异的能力。 |

## 录制备选方案（如多文件任务超时）
- 用 `python examples/demo_multi_turn.py` 预录；`/pm` 那段（在 chat 会话里）用 flash 档彩排（省 token），正式录 pro 档；
- 单任务可用 `examples/live_smoke.py` 缩短，省时间给架构/PM/评测。

## 必须出现的 4 个自研点（评委视角核对）
1. 上下文管理（/compact、auto-compact、AGENT.md 约定）
2. 工具定义与本地执行（edit_file、bash、repo-map 定位）
3. 模型输出解析（原生 tool calling + 文本兜底）
4. 循环终止与错误处理（/cost、token 预算、/undo、失败恢复）

## 后期清单
- 隐藏任何 API key（检查画面与录制文件）；
- 字幕/标注：工具名、PASS/FAIL、judge 分数、PM 产物；
- 时长≤120s，mp4，≤200MB；可加速与剪辑。
