# 2 分钟演示视频脚本（≤120 秒，mp4 ≤200MB）

> 目标：展示 agent 完成一个**真实多文件任务**（含调试+开发），并讲清"它为什么这样运转"。
> 录制环境建议：Windows Terminal + 等宽字体 + 深色主题；`coding-agent chat` 交互式演示；可 1.5-2 倍速播放关键环节（题目允许剪辑与加速）；字幕标注工具调用；全程不出现 API key。

## 时间轴

| 时间 | 画面/操作 | 旁白（中文） |
|---|---|---|
| 0:00-0:08 | 开场：终端输入 `coding-agent chat`，出现交互提示 | 这是一个我完全自研的编程智能体：它自己读文件、改代码、跑命令，直到完成任务。 |
| 0:08-0:15 | 展示工作目录 demo_project（多文件：src/stats.py、src/main.py、tests/） | 这次的任务是多文件项目：修一个方差计算的 bug、实现中位数函数、给 CLI 加 --stats 功能，还要让测试全过。 |
| 0:15-0:45 | 第一轮任务下发（加速），字幕标注：list_dir→read_file→edit_file→bash pytest | 看它的闭环：先探索目录，读源码，用 edit_file 精确修复，跑测试验证。注意工具调用都是它自主选择的。 |
| 0:45-1:00 | 追问"改了哪些文件"（加速），展示第二轮回答 + `/status`、`/cost` | 交互式会话能记住上下文多轮追问；/cost 显示用了多少 token、多少步。 |
| 1:00-1:25 | 切到架构图（静态图/动画）：system prompt+history→LLM→解析→工具执行→回填→终止判定→循环 | 核心循环是自研的：模型原生 tool calling 解析，工具本地执行，结果回填，多重条件终止。题目要求的上下文管理、工具、解析、终止、错误处理，全部自己实现，没有用任何 agent 框架。 |
| 1:25-1:50 | 切到评测：`python -m eval tasks`，展示 6 个梯度任务全部 PASS + LLM judge 评分报告 | 最特别的是自带 AI 评测系统：6 个梯度任务，隐藏测试判 PASS/FAIL，再用大模型做裁判打质量分——agent 行不行，用数据说话。 |
| 1:50-2:00 | 收尾：最终测试 7 passed + 一句话 | 自研闭环、可评测、可辩护。这就是我的编程智能体。 |

## 录制备选方案（如多文件任务超时）
- 用 `python examples/demo_multi_turn.py` 预录完整过程，视频中剪辑关键片段；
- 或改用 `examples/live_smoke.py`（单文件修 bug）缩短为 40 秒任务，把省下的时间给架构讲解与评测部分；
- 建议真实录制前先用 flash 档彩排一遍（省 token），正式录制用 pro 档保证效果。

## 必须出现的 4 个自研点（评委视角核对）
1. 上下文管理（/compact、历史延续）
2. 工具定义与本地执行（edit_file、bash）
3. 模型输出解析（原生 tool calling）
4. 循环终止与错误处理（/cost 的步数、失败恢复）

## 后期清单
- 隐藏任何 API key（检查画面与录制文件）；
- 字幕/标注：工具名、PASS/FAIL、judge 分数；
- 时长≤120s，mp4，≤200MB；可加速与剪辑。
