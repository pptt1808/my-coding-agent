# 开发文档：构建编程智能体（Coding Agent）— 定稿 v2

> 依据《软件工程专业推免项目：构建编程智能体》题目制定，并经 grill-me 定稿。本版新增 **最小闭环/进阶功能拆解** 与 **spec-driven 开发流程**。
> v2 变更：将功能拆为「最小闭环（必须完成）」与「进阶功能（分级渐进）」，并把 **spec-driven** 确立为我们开发 agent 工程本身的统一方法论。

---

## 0. 题目约束（硬性红线，不可违反）

- 独立设计并实现一个 **coding agent**：与大模型交互、自主读写文件、执行命令、完成交给它的编程任务。
- **禁止**：对现成 agent 产品封装界面；使用任何 agent 框架/SDK（LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等）。
- **允许**：模型厂商 API 客户端库、OpenAI 兼容网关、模型原生 tool calling 接口；模型、语言不限。
- **必须自研**：对话历史与上下文管理、工具定义与本地执行、模型输出解析、循环终止条件、错误处理。
- **禁止**：依赖 API 服务端托管的代码执行/文件工具（Code Interpreter、Files API）。
- API key 一律通过环境变量或未入库配置文件，不得进入仓库/README/视频。
- 截止：**2026-09-02 24:00（北京时间）**。三份提交物：公开 Git 仓库、README.txt（≤1000 汉字）、≤2 分钟 mp4 视频（≤200MB）。面试会追问"为什么这样运转、能否为设计决策辩护"。

---

## 1. 已确认的核心决策（grill-me 定稿汇总）

| # | 决策点 | 结论 |
|---|---|---|
| B1 | 技术栈 | **Python**（agent 主体 + 评测脚本，依托 pytest 判定） |
| B2 | 模型接入 | **统一 OpenAI 兼容网关**（`base_url` 一行切换）；开发用便宜模型、演示/评测用强模型 |
| B3 | 评测任务 | **自建任务集**（SWE-bench 式：仓库种子 + 隐藏测试 + 验收标准） |
| B4 | 安全模型 | **工作目录内自由 + 危险命令黑名单**；docker 沙箱留作后期增强 |
| B5 | 交付档位 | **做满全流程（Phase0–7）** |
| B6 | 演示任务 | **修一个真实小 bug**（贴评测 + 2 分钟能讲清） |
| B7 | 上下文管理 | **全量携带 + 超限摘要/截断 + 大输出截断标记** |
| B8 | 判分口径 | **确定性测试（PASS/FAIL）+ LLM-judge rubric 并行** |
| B9 | 评测隔离 | **独立临时目录 + venv**；docker 容器作可选增强 |
| B10 | 工具解析 | **原生 tool calling 优先 + 文本解析兜底** |
| B11 | 循环终止 | **多重阈值组合**（步数/工具调用数/显式完成/无进展/超时） |
| B12 | 核心亮点 | **自动评测系统**作为差异化与面试辩护支点 |
| B13 | spec-driven | **仅用于"我们开发 agent 工程本身"**（先写规格+契约测试，再实现，再验证），agent 运行时不受此约束 |

---

## 2. 总体设计目标

做一个"能自己看仓库、改代码、跑测试、反复试错直到完成任务"的 CLI agent；并且用一套**自动评测系统**客观衡量它到底行不行。评测不只是附属品，而是本项目评分与自辩的核心证据。

**一句话定位**：`一个兼容多模型网关、自研工具循环与上下文管理的编码 agent，外加一套能客观证明它 "行不行" 的 AI 可驱动自动评测系统`。

---

## 3. 可参照的开源项目（调研结果）

### 3.1 编码智能体（借鉴其工程结构，而非代码）
| 项目 | 仓库 | 借鉴点 |
|---|---|---|
| SWE-agent（Princeton） | https://github.com/SWE-agent/SWE-agent | **Agent-Computer Interface (ACI)**：为模型封装专门命令层的思路 |
| OpenCode（sst） | https://github.com/sst/opencode | 终端 TUI 化；工具注册 + 本地执行 + 会话管理（用其交互与工具抽象） |
| Aider | https://github.com/Aider-AI/aider | 纯命令行 repo 编辑；git 集成 & 自动 commit；上下文裁剪策略 |
| OpenHands（前 OpenDevin） | https://github.com/All-Hands-AI/OpenHands | 完整 agent 运行时 + 事件流 + 沙箱执行；工具层与安全隔离设计 |
| Anthropic Claude Code | （闭源，作形态参照） | 交互式会话、权限确认、工具调用循环的产品形态参考 |

### 3.2 自动评测系统（"AI 可驱动评测"）
| 项目 | 仓库 | 借鉴点 |
|---|---|---|
| SWE-bench | https://github.com/SWE-bench/SWE-bench | 标准：任务=issue，解=对 repo 的 diff/patch，判定=隔离环境跑测试 → PASS/FAIL |
| Terminal-Bench | https://www.tbench.ai/ | 在真实 CLI 环境评测 agent 完成命令行任务 |
| agent-eval-harness（opendatahub） | https://github.com/opendatahub-io/agent-eval-harness | "tool once with your pass/fail judgment and a thorough rationale"：LLM 作 judge 的打分脚本结构 |
| LLM-as-judge / G-Eval | （学术方法） | 用 rubric + 评分标准让大模型给主观维度打分 |

> 设计原则：**能跑测试的用确定性测试（SWE-bench 式），测不到的用 LLM-judge 补分（rubric）**，两条腿并行。

---

## 4. 功能拆解：最小闭环（必须完成）

> **最小闭环** = 让 agent 能够"接任务 → 读代码 → 改代码 → 跑测试 → 通过 → 停止"端到端跑通所必须的最少功能集。这一档完成，你就有"一个能用的最小 coding agent"。

### 4.1 最小闭环必须包含
| 项 | 内容 | 为什么必须 |
|---|---|---|
| N1 | **配置 + 单模型客户端**：env 读 API key / base_url / model；单次补全 + **原生 tool calling** 调用 | 一切的起点 |
| N2 | **Agent loop 骨架**：`system prompt + history → LLM → 解析 tool call → 本地执行 → 结果回填 → 终止判断 → 循环` | 最核心引擎 |
| N3 | **3 个必需工具**：`read_file`、`write_file`、`bash`（shell 执行） | 有它们就能读/改/跑测试，凑够一个任务闭环 |
| N4 | **原生 tool calling 解析**（结构化入参） | 模型→工具的安全桥梁 |
| N5 | **基础终止条件**：最大步数 + 显式"完成"信号 | 防止死循环/无限调工具 |
| N6 | **基础错误处理**：工具调用失败 → 回填错误信息给模型 | 失败不被卡死 |
| N7 | **基础上下文**：携带会话历史 + 大输出（长文件/长命令）截断标记 | 控制 token、可解释 |

### 4.2 最小闭环的"验收证明"
- 跑通一个**端到端 demo 任务**：让 agent 修复一个带隐藏测试的小 bug，并**跑通隐藏测试**。此场景既是 MVP 证明，也是后续演示视频素材。

---

## 5. 功能拆解：进阶功能（分级渐进）

> 进阶功能在最小闭环稳定后，按优先级从 T1→T5 增量叠加。每级都能独立交付、独立演示价值。

### T1 — 健壮性（换模型/弱模型依旧可靠）
- 工具集扩充：`edit_file`（精确替换/上下文）、`grep`、`glob`、`list_dir`、`append_file`。
- **文本解析兜底**：原生 tool calling 失败/畸形时，回退正则/JSON 解析。
- **多重终止**：无实质进展检测、连续失败阈值、超时（在步数+完成基础上叠加）。
- **错误处理完善**：工具超时、重试与降级。
- **上下文管理升级**：Token 计数、历史摘要、选择性组装相关片段。

### T2 — 安全与隔离
- 权限模型：工作目录白名单、危险命令黑名单（`rm -rf`、`git push --force`、高风险命令）。
- 可选交互模式：逐命令确认。
- 可选增强：docker 容器内执行命令，防止 agent 破坏宿主/泄露 key。

### T3 — 多模型网关
- 多厂商抽象：一家 `base_url` + `model` 切换，配置化 profile（开发用便宜、演示/评测用强）。

### T4 — 完备的自动评测系统（差异化核心）
- **任务集构建器**：种子仓库 + 隐藏测试 + 验收标准；支持 AI 生成任务以扩大规模。
- **完整 harness**：独立临时目录 + venv 隔离、diff/patch 应用、完整轨迹记录。
- **LLM-judge rubric 打分**：对测试覆盖不到的质量维度打分。
- **失败归因 / 趋势分析**：指出 agent 卡在哪个工具/哪步。
- **报告聚合**：通过率、耗时、token 数、各维度分数。

### T5 — 产品化与交付
- 交互式会话、可读输出/TUI、git 自动 commit。
- README.txt（≤1000 汉字）、2 分钟演示视频、面试自辩材料。

---

## 6. spec-driven 开发流程（贯穿全程）

> **定位**：spec-driven **只约束我们开发 agent 工程本身**，不约束 agent 运行时的行为。我们把"先写规格、先写验收测试、再实现、再验证"作为每个组件/每级功能的统一开发循环。

### 6.1 spec-driven 循环（每写一个组件/功能都走一遍）
1. **Spec（规格说明）**：每组件一个 markdown，写明——职责、接口（函数签名/工具 schema）、行为与边界、**可自动验证的验收标准**。
2. **契约测试（acceptance/contract tests）**：把验收标准编码成可运行的 pytest（"spec 即 oracle"）。测试必须写**在实现之前**（红色 → 绿色）。
3. **实现（Implement）**：写代码以满足 spec。
4. **验证（Verify）**：跑契约测试，全绿 = spec 满足。
5. **回归（Regress）**：spec 作为活契约，任何改动后必须重新跑全部契约测试保持绿。

### 6.2 目录约定
```
specs/            # 每个组件/功能的 spec（字段 + 验收标准）
  agent-loop.md   tools.md   context.md   termination.md   ...
tests/specs/      # 与 spec 一一对应的契约测试（pytest）
  test_agent-loop.py  test_tools.py  test_context.py  test_termination.py ...
```

### 6.3 spec 模板（示例：read_file 工具）
```markdown
## read_file
职责：读取指定路径的文件文本。
接口：read_file(path: str) -> str
行为：
  - path 相对/绝对路径均可；
  - 文件不存在 → 返回明确错误信息（不抛异常崩溃）；
  - 超长文件 → 截断并加“...已省略”标记。
验收标准（可测试）：
  A1 正常读文件返回全文；
  A2 读不存在文件返回错误文本且不崩溃；
  A3 超长文件被截断且带省略标记。
```
```python
# tests/specs/test_tools.py  （契约测试，先写）
def test_read_file_existing(tmp_path): ...
def test_read_file_missing(tmp_path): ...
def test_read_file_truncation(tmp_path): ...
```

### 6.4 与自动评测系统的关系（两层测试，各司其职）
- **Dev 层契约测试（`tests/specs/`）**：验证**我们实现的每个组件**符合其 spec。确定性、快、面向实现正确性。
- **Eval harness（`eval/`）**：验证**运行中的 agent** 能否解决真实任务（SWE-bench 式 + LLM-judge）。面向"agent 行不行"。
- **统一性**：spec 的验收标准尽可能写成可自动判定（能被 pytest / 隐藏测试 覆盖），这样 dev 契约测试与 eval 的"隐藏测试"精神一致，减少凭感觉验收。

---

## 7. AI 可驱动的自动评测系统（详细设计）

**目标**：客观、可复现地证明"这个 agent 做的编程任务到底成不成"，并提供数据支撑面试辩护。

### 7.1 组成
1. **任务定义层**：一个任务 = `{ 仓库快照/种子代码, 任务描述, 隐藏测试/验收标准, 基线 }`。可手工编写，也可 AI 生成（扩大规模）。
2. **运行层（harness）**：每个任务一个隔离执行环境（独立临时目录 + venv）。注入 agent → 干净工作区完成任务 → 完整记录轨迹。
3. **评估层**：确定性评估（跑测试 PASS/FAIL）+ 主观评估（LLM-judge rubric）。
4. **报告层**：聚合结果（通过率、平均耗时、token 数、各维度分数），生成可读报告。

### 7.2 AI 可驱动的体现
- **AI 生成任务**：把需求/bug 提示词转成"带隐藏测试的任务集"。
- **AI 作 judge**：对测不出的质量维度（代码质量、是否贴题、最小改动）打分。
- **AI 失败归因**：指出 agent 卡在哪个工具/哪步决策，形成改进方向。

### 7.3 判分口径（并行）
- **机械判定**：`tests pass → PASS`，否则 FAIL。（SWE-bench 式，最客观）
- **judge 判定**：`rubric 多维度评分`（正确性、完整性、代码质量、最小改动、符合约定，0-5）。（主观补充）

---

## 8. 里程碑与风险

- **里程碑**：`最小闭环（4）→ 契约测试覆盖（6）→ 稳健性（T1）→ 完整评测（T4）→ 产品化/演示（T5）`。**最小闭环最先打通**，后续逐级叠加。
- **风险 1（时间）**：做满全流程时间紧张 → 滚动交付：最小闭环先稳，评测紧跟，文档收尾。
- **风险 2（评测任务）**：任务集不能稳定判定会削弱说服力 → 优先选"有明确隐藏测试、跑一遍 PASS/FAIL"的任务。
- **风险 3（换模型）**：弱模型/网关下 tool calling 可能失灵 → 已定"原生+文本兜底"（T1 必做）。
- **风险 4（API key 安全）**：→ 一律环境变量/未入库配置；提交前审计仓库与视频。
