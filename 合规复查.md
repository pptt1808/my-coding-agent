# 题目合规复查报告

> 复查时间：2026-08-27。对照《软件工程专业推免项目：构建编程智能体》题目逐条核对，附证据（grep 结果与 git 状态）。

## 1. 依赖清单（pyproject.toml / requirements.txt）
| 依赖 | 用途 | 合规 |
|---|---|---|
| `openai>=1.40.0` | 模型厂商 API 客户端库（OpenAI 兼容网关） | ✅ 题目明确允许 |
| `python-dotenv>=1.0.0` | 读取未入库 .env 配置 | ✅ 允许 |
| `pytest / pytest-cov`（dev） | 契约测试 | ✅ 开发工具 |

**无任何 agent 框架/SDK**：LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等关键词全仓（115 个跟踪文件）零命中。

## 2. 禁止项逐条核对
| 规则 | 结论 | 证据 |
|---|---|---|
| 禁止封装现成 agent 产品界面 | ✅ 通过 | 子进程仅调用本地 `python`/`pytest`（6 处），无 claude/codex/opencode/aider 等外部 agent 二进制调用 |
| 禁止 agent 框架/SDK | ✅ 通过 | 全仓 import 250 处：仅 stdlib + 自研模块 + openai/dotenv/pytest；框架名仅在**文档**中作为设计参考出现（开发文档/迭代分析/README/specs），代码零引用 |
| 禁止依赖服务端托管的代码执行/文件工具 | ✅ 通过 | 唯一外部 API 调用为 `agent/llm.py:63` 的 `chat.completions.create(**kwargs)`（原生 tool calling）；无 Files API、无 code_interpreter、无 responses.create、无 assistants/beta |
| API key 不入库/README/视频 | ✅ 通过 | 跟踪文件 115 个，`git ls-files` 无 `.env`；全仓无 `sk-` 密钥模式；key 仅存于 gitignore 的 .env |

## 3. 必须自研的四项 + 错误处理（对照实现文件）
| 必须自研 | 实现位置 | 说明 |
|---|---|---|
| 对话历史与上下文管理 | `agent/history.py`、`agent/compact.py`、`agent/loop.py` | 全量携带 + 大输出截断（尾部优先）+ 启发式压缩 + LLM 摘要 /compact + 自动 compact |
| 工具定义与本地执行 | `tools/schemas.py`、`tools/registry.py`、`tools/file_ops.py`、`tools/shell.py` | JSON schema 定义 8 工具、本地执行、workdir 围栏、危险命令黑名单、超时 |
| 模型输出解析 | `agent/parser.py` | 原生 tool calling 优先 + `<tool>` 文本兜底，失败不崩溃 |
| 循环终止条件 | `agent/termination.py` | 多重阈值：完成/步数/工具数/连续失败/无进展（同工具+同参数）/超时 |
| 错误处理 | `agent/errors.py` + `tools/registry.py:dispatch` | 错误统一转字符串回填模型，循环不崩溃 |

## 4. 提交物规格
| 提交物 | 规格 | 当前状态 |
|---|---|---|
| Git 仓库 | 公开、保留完整提交历史、截止后不再推送 | 本地 **36 条提交**、历史完整；**待推送公开仓库** |
| README.txt | ≤1000 汉字 | **259 汉字** ✅ |
| 视频 | ≤2 分钟、mp4 ≤200MB | 脚本已备（视频脚本.md）；**待录制** |

## 5. 结论
**合规**。未使用任何现成 agent SDK/框架/产品；四个必须自研点全部自行实现并有 **179 个契约测试**佐证（新增 `pm`/`repomap`/`multi` 模块已复核：仅 stdlib + 自研 + openai/dotenv，无 agent 框架）；无凭据入库（`.env` 未跟踪，无 `sk-`）；README.txt 符合字数要求。剩余人工步骤仅为：推送公开仓库、填仓库地址、录制视频、打包提交。
