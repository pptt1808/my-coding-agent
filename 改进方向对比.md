# 与成熟开源项目的对比分析 & 改进方向

> 对照 Claude Code / OpenCode / SWE-agent / Aider 等成熟实现。**诚实原则**：不吹自己，用"我们有什么 vs 缺什么 vs 我们独有的"来定方向。

## 一、我们做得不错 / 甚至有优势的
| 能力 | 说明 | 对比 |
|---|---|---|
| **自动评测体系**（差异化核心） | SWE-bench 式隐藏测试 + LLM-judge 质量分 + 成本/耗时/token 全记录 + 断点续跑 | Claude Code/OpenCode **不内置**这类评测器；我们把它做成头等公民 |
| **PM 演示模式** | 澄清门(一次一问)→spec→确认→MVB→validate→story→pitch，产物抗污染 | **无成熟 agent 有此**（我们的原创个性化） |
| **spec-driven 工程纪律** | 每功能先 spec→契约测试(红)→实现(绿)；166 测试；git 按功能提交 | 多数 agent 项目没这么系统 |
| **成本控制** | token 预算上限、auto-compact（Claude Code window）、重试、可续跑 | 与成熟做法对齐且实测过(126k→73k) |
| **模型分层 + A/B 实证** | flash/pro 分层，用大任务 A/B 量化（发现多 agent 对中量任务负收益） | 用它**否决**了不划算的方向(Phase C) |

## 二、我们落后于成熟项目的主要短板（按价值排序）
| # | 短板 | 成熟实现 | 价值/依据 | 优先级 |
|---|---|---|---|---|
| **1** | **代码库语义概览（repo-map）** | [Aider Repository Map](https://aider.chat/docs/repomap.html)：用符号图(类/函数/关系)做一个**紧凑的代码地图**喂给模型，不用重读全库 | **最高**——直接解决我们"探索大仓库贵/上下文膨胀"的根因；比我们现在的"grep/glob 全量嗅探 + 全量历史"高一档 | **高** |
| **2** | **Undo / rewind / 检查点** | [Claude Code](https://www.anthropic.com/news/enabling-claude-code-to-work-more-autonomously) 的 rewind/checkpoints；[OpenCode](https://learnopencode.com/en/5-advanced/19-lsp) /undo-redo | **高**——交互式里"撤销 agent 上一次改动"对建立信任极其重要，我们完全没有 | **高** |
| **3** | **持久项目上下文（记忆）** | Claude Code 读 `CLAUDE.md`（项目约定） | **中高**——让 agent 记住项目约定；我们的 PM 模式可复用为 `PM_PROFILE` | 中 |
| **4** | **通用循环的 plan/think 模式** | Claude Code plan 模式（先想/给方案→批准→执行） | 中高——我们只在 PM 模式有澄清门，通用循环没有"先规划再动手" | 中 |
| **5** | **语法感知编辑（LSP/tree-sitter）** | [OpenCode LSP](https://www.opencode.asia/source-code/lsp/)：基于语言服务器的精确编辑 | 中——我们 `edit_file` 是正则替换，大重构时易错 | 中 |
| **6** | **docker 沙箱隔离** | OpenHands / Claude Code 在容器内执行 | 中——我们只有 workdir 围栏+黑名单；更强隔离可后期加 | 低(按需) |
| **7** | 后编辑自动校验门 | Claude Code 改完自动跑 lint/test | 中——我们 agent 会跑测试但非确定性门 | 低(已有部分) |

## 三、我们"已经权衡过并放弃/暂缓"的
- **循环内动态派生子 agent（supervisor / Phase C）**：成熟 agent(Claude/OpenCode 用 agent 委派)，但我们 A/B 实测**对中量任务负收益**（多 agent +283% token/+75% 时间），故**不追**——用数据否定的，不是忘了。

## 四、建议的改进优先级（结合价值 & 我们的定位）
1. **先做 #1 repo-map**（[Aider 思路](https://aider.chat/docs/repomap.html)）：给探索/实现 agent 一个紧凑代码地图 → 直击"探索贵、上下文膨胀"根因，且和我们已有的"auto-explore + brief"契合；
2. **再做 #2 undo/rewind**：git 瞬态检查点 + `/undo`，显著提升交互信任；
3. **顺手 #3 持久上下文**：项目 `AGENT.md`（约定）+ PM 模式读 `PM_PROFILE`，与我们的 /task、会话持久化打通；
4. #4 plan 模式可把 PM 的澄清门下沉到通用循环（低成本复用）。

> 定位说明：我们的**独特价值**是"自带 AI 评测 + PM 演示模式 + spec-driven 纪律"；**补齐成熟项目的通用短板**（尤其 repo-map）能让它**既独特又扎实**，而不是"只有花活"。
