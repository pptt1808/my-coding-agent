# 使用说明（快速上手体验）

> 本机已就绪：`.env` 已配好 DeepSeek key（flash=开发 / pro=评测）、依赖已安装、123 个契约测试通过。
> 两种运行方式任选：
> - `python -m agent.cli ...`（项目目录内，无需 PATH）
> - 把 `C:\Users\lion\AppData\Local\Python\pythoncore-3.14-64\Scripts` 加入 PATH 后用 `coding-agent ...`

## 0. 怎么选工作区？（与其他 coding agent 一致）

三种方式，启动时第一行会显示当前工作区：

```bash
# 方式一（推荐，和 Claude Code 一样）：cd 进你的项目再启动，工作区 = 该目录
cd D:\你的项目
coding-agent chat

# 方式二：从任意位置显式指定工作区
coding-agent chat --workdir D:\你的项目

# 方式三：环境变量 WORKDIR
set WORKDIR=D:\你的项目 && coding-agent chat
```

`.env` 查找顺序：当前目录 `.env` → 本项目根 `.env` → `~/.coding-agent/.env`。
所以即使 cd 到别的项目，也会自动用上本项目已配好的 key。

## 1. 交互式体验（推荐，像和 Claude Code 聊天）
```bash
cd D:\Desktop\编程agent
python -m agent.cli chat
```
- 直接输入中文/英文任务，agent 会自主读文件、改代码、跑命令；
- 斜杠命令：`/help` `/clear` `/compact`（压缩上下文）`/status` `/cost`（token 用量）`/model deepseek-v4-pro`（切强模型）`/task add 步骤`（任务清单）`/save 名字` `/resume 名字`（会话存档/恢复）`/review`（自查改动）`/permissions`（查看黑名单）
- 工作目录 = 启动目录（或 .env 的 WORKDIR）；想让它"只动某个文件夹"，先 cd 过去再启动。

## 2. 一次性任务
```bash
python -m agent.cli run "修复 src/util.py 里 add 函数的 bug，让它返回 a-b，并跑测试验证"
python -m agent.cli run "..." --no-stream   # 关闭流式输出
```
⚠️ Windows 下任务串里**别用英文双引号**（会被命令行拆参），用单引号或中文引号。

> 流式输出：chat 与 run 默认把模型 token 即时打印（像 ChatGPT 打字效果），`--no-stream` 或 `.env` 里 `STREAM=0` 可关。
> 指令菜单：交互式里直接输 `/` 回车即显示全部可用命令；也支持前缀展开，如 `/comp` → `/compact`、`/sta` → `/status`。

## 3. 自动评测
```bash
python -m eval tasks              # 6 级梯度任务全部跑一遍
python -m eval tasks --task lru_cache   # 只跑某个任务
```

## 4. 三分钟体验路线
1. `python -m agent.cli chat`；
2. 输入：`在这个目录里创建一个 Python 脚本 fib.py，实现斐波那契函数，然后写个测试并运行验证`；
3. 看它自主完成：读/写文件 → 跑测试 → 汇报；
4. `/cost` 看 token；`/compact` 压缩后再问下一件事；`/task add 下一个步骤` 试试任务清单。

## 5. 支持哪些语言？
**能力不限语言**：agent 的工具只读写文件+执行命令，模型本身通晓几乎所有主流语言——你说什么语言它就写什么语言。
**能否"运行验证"取决于本机工具链**：

| 语言 | 本机状态 | 能否生成 | 能否运行验证 |
|---|---|---|---|
| Python | 3.14.5 ✅ | ✅ | ✅（pytest） |
| JavaScript/TypeScript/Node | v24.13.0 ✅ | ✅ | ✅（node/npm） |
| Java | 17 ✅ | ✅ | ✅（java/javac） |
| Go / Rust / C++(gcc) / .NET | 未安装 ❌ | ✅（会生成代码） | ❌（命令会报"无法识别"） |
| Shell/Batch、SQL、HTML/CSS、配置文件等 | — | ✅ | 按命令而定 |

> 已实测验证的只有 Python（全部测试/评测/演示）。想体验 Node/Java：直接在工作目录里给任务"写一个 Node.js 脚本 xxx，用 node 运行验证"，模型会自己调用 node。

## 6. 遇到问题
- 模型命令反复失败 → 通常是在 Windows cmd 下用了 Unix 语法（已内置提示，若仍发生可 `/permissions` 查看或换写法）；
- 卡住/超时 → 终止条件会自动止损，重发任务即可；
- 想让它更"强" → `/model deepseek-v4-pro`。
