# Task: fix_add_bug

Fix the bug in `src/util.py`: the function `add()` currently returns `a + b`,
but it should return `a - b`. The repo has a hidden test `test_util.py` that
checks `add(10, 4) == 6`. Make the tests pass before finishing.
