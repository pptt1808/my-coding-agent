def evaluate(expr: str) -> float:
    """Evaluate an arithmetic expression with + - * / and parentheses.

    Normal precedence (* / bind tighter than + -), left-associative,
    whitespace tolerated. Raises ValueError on invalid expressions or
    division by zero. Implement it.
    """
    raise NotImplementedError
