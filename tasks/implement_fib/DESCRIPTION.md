# Task: implement_fib

Implement the `fib(n)` function in `src/fib.py` so it returns the n-th
Fibonacci number: fib(0)=0, fib(1)=1, and for n >= 2,
fib(n) = fib(n-1) + fib(n-2). The hidden test checks several values; make them
pass before finishing.
