# 面试自辩材料：为什么它这样运转

> 面试重点：你是否理解 agent 为什么这样运转、能否为设计决策辩护。本文件按"题目要求自研的四个方面 + 评测体系 + 工程方法 + 尖锐问题预答"组织，全部有真实证据（提交历史、测试、评测数据）支撑。

## 0. 一句话定位
一个**完全自研**的编码 agent（OpenAI 兼容网关 + 模型原生 tool calling），外加一套**能客观证明它行不行的 AI 自动评测系统"。

## 1. 四个必须自研的点：怎么设计、为什么、踩过什么坑

### 1.1 对话历史与上下文管理（agent/history.py, agent/compact.py）
- **设计**：默认全量携带历史；大输出（长文件/命令）截断并保留尾部（错误/结果在末尾）；`summarize_oldest` 启发式压缩兜底；`/compact` 用 LLM 生成结构化摘要（目标/事实/已完成/待做/下一步）替换历史；`AUTO_COMPACT_AT_TOKENS` 支持循环内自动压缩。
- **为什么**：全量携带最简单可靠、可解释；截断/摘要控制 token 防超限；LLM 摘要保真度高，对抗长会话上下文衰减。
- **踩坑（有测试）**：`summarize_oldest` 曾因"摘要头与原文等长"死循环 → 改为 50 字符头 + 无法压缩则丢弃（契约测试 CT5 覆盖）。

### 1.2 工具定义与本地执行（tools/）
- **设计**：JSON schema 定义 8 个工具（read/write/edit/append/bash/list_dir/glob/grep），registry 注册分发；所有路径经 workdir 围栏解析（防 `..` 逃逸）；bash 带超时 + 危险命令黑名单；输出尾部优先截断；`--tools` 可白名单裁剪。
- **为什么**：schema 驱动让模型原生 tool calling 直接可用；围栏保证"只动工作目录"；黑名单防误删。
- **踩坑（多轮测试发现）**：Windows cmd 不支持 heredoc/Unix find，模型浪费步骤 → 在 bash schema 描述与系统提示中明示平台（parser_calc 单任务 token 从 163k 降到 131k）。

### 1.3 模型输出解析（agent/parser.py）
- **设计**：优先模型**原生 tool calling**；若网关/弱模型返回畸形或纯文本，回退解析 `<tool name=...>{json}</tool>` 文本协议；解析失败返回 None 不崩溃。
- **为什么**：双路径兼容多厂商/多模型（B10）；文本兜底保证换弱模型不挂。
- **验证**：P1-P4 契约测试 + 真实跑通 OpenAI 兼容网关（DeepSeek flash/pro 均返回结构化 tool_calls）。

### 1.4 循环终止条件与错误处理（agent/termination.py, agent/errors.py）
- **设计**：多重阈值组合——显式"完成"、最大步数、最大工具调用数、连续失败数、**无进展（同工具+同参数重复）**、超时；工具/解析错误一律转成字符串回填给模型，循环不崩溃。
- **为什么**：单一条件会漏（模型说完成但没做完）或误杀（探索被当无进展）；错误回填让模型自我纠错。
- **踩坑（梯度评测发现，最有价值）**：无进展原来只看工具名——连续读 3 个不同文件被误判"无进展"导致提前终止（demo 第一版 Turn1 中断但改动已生效）→ 改为同工具+同参数才算无进展，新增契约测试 L6/L7。

## 2. 自动评测体系（差异化核心）
- **结构**：任务集（`tasks/`，6 级梯度 L1-L6：修 bug→实现函数→跨文件重构→LRU→表达式解析器→微妙二分边界 bug）→ 隔离临时目录运行 → 隐藏测试 pytest 判 PASS/FAIL → 无标准测试的质量维度用 LLM-judge 打 rubric 分 → 报告（通过率/耗时/token/各维度均分）。
- **为什么**：SWE-bench 式机械判定最客观；LLM-judge 补足"代码质量/最小改动"等测试测不出的维度（实测 judge 抓出过整文件覆写导致 minimal=2.0）；所有任务种子状态离线验证必失败，保证任务有效。
- **分层**：MODEL=flash（开发，快省）、EVAL_MODEL=pro（评测，稳），同一评测可量化两档差异（pro avg 11.6s/8.5k token vs flash avg 5.7s/6.6k token，6 任务全 PASS）。

## 3. 工程方法：spec-driven（题目规则 2"须对每处设计负责"）
- 每个组件先写 spec（接口+可自动验证的验收标准），**先写契约测试（红）再实现（绿）**；
- 123 个契约测试全绿；git 提交历史 13 条可追溯；每个踩坑都有对应测试防回归。

## 4. 尖锐问题预答
**Q：为什么不直接用 LangChain/现成框架？**
A：题目明确禁止，且自研才能讲清"为什么这样运转"。我的循环只有一层：system prompt+history→LLM→解析→工具→回填→终止，没有框架黑盒。

**Q：你的 agent 和 Claude Code 有什么区别？**
A：Claude Code 是闭源产品；我的是自研循环（题目要求的四件事全部自己写）+ 自带可量化评测（6 级任务+LLM judge）。评测让"行不行"有数据，不只是演示。

**Q：评测能证明能力吗？**
A：梯度任务覆盖从单行修复到微妙边界 bug；隐藏测试种子必失败、隔离运行防污染；PASS/FAIL 是机械判定。诚实边界：任务集是合成小任务，不等于真实大型项目，但体系可扩展到任意仓库（SWE-bench 式）。

**Q：agent 卡住/失败怎么办？**
A：多重终止条件保证不无限跑；错误回填让模型纠错；评测报告会记录失败与轨迹，可归因（卡在哪个工具/哪步）。

**Q：换模型会坏吗？**
A：网关 + 原生 tool calling，解析失败有文本兜底；分层配置可一键切换 flash/pro（实测都通过）。

**Q：遇到长任务会失控吗？**
A：不会。auto-compact（Claude Code 窗口，默认开）+ `MAX_TOTAL_TOKENS` 预算（AgentBudget 式）+ 多步终止条件；实测一个案例从 126k 降到 73k tokens。反复测试还暴露并修掉了"无进展只看同工具+同参数、被花样探针骗过"等真实问题。

**Q：为什么多 agent 不默认开？**
A：用我们自己的 A/B 实测（47 文件任务）：多 agent +283% token / +75% 时间、结果相同——**数据否定**了"对中量任务用多 agent"，所以默认 off、高阈值、按需 `--explore`。这是"有数据支撑的取舍"，不是忘了。

**Q：api key 安全？**
A：只存于 gitignore 的 .env，从未入库（`git ls-files` 验证，新增模块也复核过无框架/无 key）。

## 5. 数据证据一览
| 证据 | 数值 |
|---|---|
| 契约测试 | **179 passed**（spec-driven：每功能先 spec→契约测试→实现） |
| 评测任务 | 6 级梯度（L1-L6）种子全失败、真实评测 6/6 PASS（pro）+ `large_task` 多模块 PASS |
| **外部基准** | **BigCodeBench 30 题 = 40%**（flash，成本可控~20k/题）；SWE-bench 适配器就绪 |
| 分层对比 | flash 2/2、pro 2/2 PASS；模型分层量化 (pro avg 11.6s/8.5k vs flash 5.7s/6.6k) |
| 成本控制 | auto-compact 默认开、token 预算、重试、可续跑；失控案例 126k→73k |
| 改进（对照成熟开源） | Aider repo-map、undo/rewind、AGENT.md/PM_PROFILE（均有契约测试） |
| 修复记录 | summarize 死循环、workdir 隔离、无进展误判、Windows heredoc、产物污染（均有测试+commit，36+ 条） |
| 差异化 | **PM 演示模式**（澄清门→spec→确认→MVP→validate→story/pitch 四件产物，无成熟 agent 有）；自带 AI 评测 |
| 代码规模 | agent/tools/eval，核心循环 ~150 行，**无 agent 框架依赖**（新增 pm/repomap/multi 已复核） |
